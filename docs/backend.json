{
  "entities": {
    "Idea": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Idea",
      "type": "object",
      "description": "Represents an idea submitted by a user.",
      "properties": {
        "idea": {
          "type": "string",
          "description": "The text content of the idea."
        },
        "creativityScore": {
          "type": "number",
          "description": "The creativity score of the idea as determined by Gemini AI."
        },
        "analysis": {
          "type": "string",
          "description": "Details from Gemini AI analysis about similar existing ideas and their applications."
        },
        "source": {
          "type": "string",
          "description": "A markdown formatted string of sources to prove the existence of the idea."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the idea was created."
        }
      },
      "required": [
        "idea",
        "creativityScore",
        "analysis",
        "source",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/ideas/{ideaId}",
        "definition": {
          "entityName": "Idea",
          "schema": {
            "$ref": "#/backend/entities/Idea"
          },
          "description": "Stores ideas submitted by any user. No authorization required for now.",
          "params": [
            {
              "name": "ideaId",
              "description": "The unique identifier for the idea document."
            }
          ]
        }
      }
    ]
  }
}
